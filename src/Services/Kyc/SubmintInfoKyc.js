import {
    getHeaders,
    http,
    errorMessage,
    successMessage,
    loader,
} from "../../utils/constants"

export default async function submitInfo({ dataSent }) {
    try {
    } catch (error) {}
}
